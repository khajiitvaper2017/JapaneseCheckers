<Window x:Class="JapaneseCheckers.Views.PlayersListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JapaneseCheckers.Views"
        xmlns:viewModels="clr-namespace:JapaneseCheckers.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="PlayersListWindow" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource MaterialWindowStyle}">
    <Border Background="White" BorderThickness="0" CornerRadius="12" Margin="20" Height="360"
            VerticalAlignment="Top">
        <StackPanel Margin="20">
            <TextBlock Margin="5">List of players: </TextBlock>
            <DataGrid  ItemsSource="{Binding PlayersData.Collection, Mode=OneWay }" AutoGenerateColumns="True" Margin="5"/>
            <TextBlock Margin="5">List of bots: </TextBlock>
            <DataGrid Margin="5" ItemsSource="{Binding BotsData.Collection, Mode=OneWay }" AutoGenerateColumns="True"/>
        </StackPanel>
        </Border>
</Window>
