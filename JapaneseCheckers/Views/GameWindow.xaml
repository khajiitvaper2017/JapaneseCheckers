<Window x:Class="JapaneseCheckers.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        xmlns:local="clr-namespace:JapaneseCheckers"
        xmlns:viewModels="clr-namespace:JapaneseCheckers.ViewModels"
        xmlns:views="clr-namespace:JapaneseCheckers.Views"
        Title="Japanese Checkers" Height="864" Width="800" ResizeMode="NoResize">
    <Window.DataContext>
        <viewModels:CheckersViewModel/>
    </Window.DataContext>
<Grid>
    <Grid.RowDefinitions>
         <RowDefinition Height="64"/> 
         <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal" Margin="10">
        
        <TextBlock Text="{Binding Path=Game.Turn, StringFormat='Ход: {0}'}"/>
    </StackPanel>
    <views:CheckersControl Grid.Row="1"/>
</Grid>
</Window>
